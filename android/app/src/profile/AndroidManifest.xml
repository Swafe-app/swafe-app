<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Add these lines -->
    <string name="permission_camera_rationale">L'accès à la caméra nous permettra de prendre un selfie pour valider votre profile</string>
    <string name="permission_location_rationale">Votre localisation nous aidera à vous montrer plus rapidement les signalements autour de vous</string>
</manifest>
